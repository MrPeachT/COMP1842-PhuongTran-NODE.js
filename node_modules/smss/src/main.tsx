import React, { PureComponent } from 'react'
import { render } from 'react-dom'

import { Home as Root } from './routes'

declare var module: any

const el = document.getElementById('root')

render(<Root />, el)

if (module.hot) {
  module.hot.accept('./routes', () => render(<Root />, el))
}
