import { textColor, paletteColor } from '../theme/light';
import { o2a } from '../utils';
export default function (colors, themeColor) {
    if (colors === void 0) { colors = textColor; }
    if (themeColor === void 0) { themeColor = paletteColor; }
    // 字体颜色
    var fc = o2a(colors)
        .map(function (_a) {
        var key = _a.key, value = _a.value;
        return ".f" + key + "{color:" + value + "}";
    })
        .join('');
    // 主题颜色
    var tc = o2a(themeColor)
        .map(function (_a) {
        var key = _a.key, value = _a.value;
        return "." + key + "{color:" + value + "}";
    })
        .join('');
    return "" + fc + tc;
};
//# sourceMappingURL=color.js.map