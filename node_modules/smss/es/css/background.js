import { paletteColor } from '../theme/light';
import { o2a } from '../utils';
export default function (themeColor) {
    if (themeColor === void 0) { themeColor = paletteColor; }
    // 主题背景
    var pc = o2a(themeColor)
        .map(function (_a) {
        var key = _a.key, value = _a.value;
        return "." + key.replace(/\b\w/g, function (l) {
            return l.toUpperCase();
        }) + "{background-color:" + value + "}";
    })
        .join('');
    return pc;
};
//# sourceMappingURL=background.js.map