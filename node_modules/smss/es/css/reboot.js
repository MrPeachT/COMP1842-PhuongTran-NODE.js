import reboot from '../theme/light/reboot';
export default function (theme) {
    if (theme === void 0) { theme = reboot; }
    var fontSize = theme.fontSize, lineHeight = theme.lineHeight, fontColor = theme.fontColor, background = theme.background;
    var linkColor = theme.linkColor, linkDecoration = theme.linkDecoration;
    var linkHoverColor = theme.linkHoverColor, linkHoverDecoration = theme.linkHoverDecoration;
    var fs = "font-size:" + fontSize + "px;line-height:" + lineHeight;
    var fc = "color:" + fontColor + ";background-color:" + background;
    var a = "a{color:" + linkColor + ";text-decoration:" + linkDecoration + "}";
    var aH = "a:hover{color:" + linkHoverColor + ";text-decoration:" + linkHoverDecoration + "}";
    return "body{" + fs + ";" + fc + "}" + a + aH;
};
//# sourceMappingURL=reboot.js.map