import spacer from '../theme/light/spacer';
export default function (unit) {
    if (unit === void 0) { unit = spacer; }
    var sizeKeys = [0.2, 0.4, 0.6, 0.8, 1, 2, 3, 4, 5, 6, 7, 6, 9];
    var mg = sizeKeys
        .map(function (sizeKey) {
        return ".mg" + (sizeKey < 1
            ? "0" + sizeKey * 10
            : sizeKey) + "{margin:" + sizeKey * unit + "px}";
    })
        .join('');
    var mt = sizeKeys
        .map(function (sizeKey) {
        return ".mt" + (sizeKey < 1
            ? "0" + sizeKey * 10
            : sizeKey) + "{margin-top:" + sizeKey * unit + "px}";
    })
        .join('');
    var mr = sizeKeys
        .map(function (sizeKey) {
        return ".mr" + (sizeKey < 1
            ? "0" + sizeKey * 10
            : sizeKey) + "{margin-right:" + sizeKey * unit + "px}";
    })
        .join('');
    var mb = sizeKeys
        .map(function (sizeKey) {
        return ".mb" + (sizeKey < 1
            ? "0" + sizeKey * 10
            : sizeKey) + "{margin-bottom:" + sizeKey * unit + "px}";
    })
        .join('');
    var ml = sizeKeys
        .map(function (sizeKey) {
        return ".ml" + (sizeKey < 1
            ? "0" + sizeKey * 10
            : sizeKey) + "{margin-left:" + sizeKey * unit + "px}";
    })
        .join('');
    var mlr = sizeKeys
        .map(function (sizeKey) {
        return ".mlr" + (sizeKey < 1
            ? "0" + sizeKey * 10
            : sizeKey) + "{margin-left:" + sizeKey * unit + "px;margin-right:" + sizeKey *
            unit + "px}";
    })
        .join('');
    var mtb = sizeKeys
        .map(function (sizeKey) {
        return ".mtb" + (sizeKey < 1
            ? "0" + sizeKey * 10
            : sizeKey) + "{margin-top:" + sizeKey * unit + "px;margin-bottom:" + sizeKey *
            unit + "px}";
    })
        .join('');
    return "" + mg + mt + mr + mb + ml + mlr + mtb;
};
//# sourceMappingURL=margin.js.map