import space from '../theme/light/spacer';
var breakpoints = {
    sm: 576,
    md: 768,
    lg: 992,
    xl: 1200,
};
var contentWidth = {
    sm: 540,
    md: 720,
    lg: 960,
    xl: 1140,
};
export default function (bps, ctw, spacer) {
    if (bps === void 0) { bps = breakpoints; }
    if (ctw === void 0) { ctw = contentWidth; }
    if (spacer === void 0) { spacer = space; }
    var container = Object.keys(bps)
        .map(function (bp) {
        return "@media(min-width:" + bps[bp] + "px){.ctn{width:" + ctw[bp] + "px;padding-left:" + spacer * 3 + "px;padding-right:" + spacer * 3 + "px}}";
    })
        .join('');
    return ".ctn{width:100%;margin-left:auto;margin-right:auto}\n    " + container;
};
//# sourceMappingURL=container.js.map