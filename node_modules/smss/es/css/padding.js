import spacer from '../theme/light/spacer';
export default function (unit) {
    if (unit === void 0) { unit = spacer; }
    var sizeKeys = [0.2, 0.4, 0.6, 0.8, 1, 2, 3, 4, 5, 6, 7, 6, 9];
    var pd = sizeKeys
        .map(function (sizeKey) {
        return ".pd" + (sizeKey < 1
            ? "0" + sizeKey * 10
            : sizeKey) + "{padding:" + sizeKey * unit + "px}";
    })
        .join('');
    var pt = sizeKeys
        .map(function (sizeKey) {
        return ".pt" + (sizeKey < 1
            ? "0" + sizeKey * 10
            : sizeKey) + "{padding-top:" + sizeKey * unit + "px}";
    })
        .join('');
    var pr = sizeKeys
        .map(function (sizeKey) {
        return ".pr" + (sizeKey < 1
            ? "0" + sizeKey * 10
            : sizeKey) + "{padding-right:" + sizeKey * unit + "px}";
    })
        .join('');
    var pb = sizeKeys
        .map(function (sizeKey) {
        return ".pb" + (sizeKey < 1
            ? "0" + sizeKey * 10
            : sizeKey) + "{padding-bottom:" + sizeKey * unit + "px}";
    })
        .join('');
    var pl = sizeKeys
        .map(function (sizeKey) {
        return ".pl" + (sizeKey < 1
            ? "0" + sizeKey * 10
            : sizeKey) + "{padding-left:" + sizeKey * unit + "px}";
    })
        .join('');
    var plr = sizeKeys
        .map(function (sizeKey) {
        return ".plr" + (sizeKey < 1
            ? "0" + sizeKey * 10
            : sizeKey) + "{padding-left:" + sizeKey * unit + "px;padding-right:" + sizeKey *
            unit + "px}";
    })
        .join('');
    var ptb = sizeKeys
        .map(function (sizeKey) {
        return ".ptb" + (sizeKey < 1
            ? "0" + sizeKey * 10
            : sizeKey) + "{padding-top:" + sizeKey * unit + "px;padding-bottom:" + sizeKey *
            unit + "px}";
    })
        .join('');
    return "" + pd + pt + pr + pb + pl + plr + ptb;
};
//# sourceMappingURL=padding.js.map